# Puerto dinámico de Render
server.port=${PORT:8080}

# ================================
# Configuración de Supabase PostgreSQL - SESSION POOLER (IPv4)
# ================================
# URL JDBC del Session Pooler (IPv4 compatible)
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.bhmcnobbizqsdplxambv
spring.datasource.password=RpPvCgRkKPYAgYEt
spring.datasource.driver-class-name=org.postgresql.Driver

# ================================
# JPA configuración
# ================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Logs SQL (apagar en producción si molesta)
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Pool de conexiones HikariCP
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# Configuración SSL específica para Supabase
spring.datasource.hikari.ssl-mode=require